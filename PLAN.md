# 🃏 러브 윈즈 올 (Love Wins All) — 개발 플랜

넷플릭스 예능 〈데스게임〉의 게임 "러브 윈즈 올"을 iOS 게임으로 구현한다.
검증된 [feast_of_memory](https://github.com/sangmin082/feast_of_memory)(기억의 만찬) 프로젝트의
아키텍처를 그대로 따르되, **히든 정보(비공개 손패)와 베팅**이라는 이 게임 고유의 요소에 맞게 엔진과 온라인 구조를 조정한다.

- **1인용** — AI와 대결 (난이도 3단계: 쉬움/보통/어려움)
- **2인용** — 방 코드 기반 실시간 온라인 대결 (Node.js 서버)
- **연습 게임** — 본 게임 전 칩 20개로 진행하는 연습 모드

---

## 1. 게임 규칙 정리

가위바위보 승부와 베팅으로 상대의 칩을 모두 빼앗으면 승리한다.

**카드 (총 30장)**: 가위 12 · 바위 7 · 보 7 · 러브 4

**족보 (높은 순)**

| 순위 | 족보 | 조건 |
|---|---|---|
| 1 | 러브 윈즈 올 | 러브 3장 |
| 2 | 트리플 | 같은 가위바위보 3장 |
| 3 | 투 러브 | 러브 2장 |
| 4 | 믹스 | 가위·바위·보 각 1장 |
| 5 | 더블 | 러브 없이 같은 가위바위보 2장 |
| 6 | 원 러브 | 러브 1장 |

3장 손패는 위 6가지 중 **정확히 하나**로 분류된다. (러브 0장이면 트리플/믹스/더블 중 하나,
러브 1장이면 나머지 2장이 무엇이든 원 러브 — "더블은 러브 없이"라는 조건 때문)

**동순위 비교**: 가위바위보 상성(가위>보, 보>바위, 바위>가위)으로 승부. 무승부면 나머지 카드의 상성으로 재비교.
- 투 러브 ↔ 투 러브: 나머지 1장의 상성으로 승부
- 원 러브 ↔ 원 러브: 즉시 무승부 (나머지 카드 비교 없음)

**라운드 진행**
1. 기본 배팅 칩 1개 → 카드 3장씩 배분
2. **1차 베팅** (통상 포커 방식: 체크/벳/콜/레이즈/폴드)
3. 각자 카드 1장 오픈 + **족보 선언** (거짓 선언 가능)
4. **2차 베팅**
5. 쇼다운 — 족보가 높은 쪽이 팟 획득 (무승부면 팟은 다음 라운드로 이월)
6. 폴드로 끝나면 카드 비공개. 부주의로 공개된 카드는 교체 없이 진행

**선(先)**: 1라운드는 추첨, 이후는 직전 라운드 승자. 시작 칩 25개(연습 게임은 20개), 상대 칩을 전부 획득하면 최종 승리.

### 규칙 해석 — 엔진 구현 전 확정할 사항

방송 규칙에 명시되지 않은 부분은 아래와 같이 정하고, 엔진 상수/설정으로 분리해 쉽게 바꿀 수 있게 한다.

| # | 쟁점 | 채택안 | 비고 |
|---|---|---|---|
| D1 | 덱 운영 | **매 라운드 30장 전체를 리셔플** 후 3+3장 배분 | 대안(라운드 간 덱 소진)은 엔진 옵션으로 남김 |
| D2 | 베팅 한도 | **노리밋** (헤즈업이라 사이드팟 불필요, 콜 못 하면 올인 + 초과분 반환) | 총 칩 50개라 폭주 위험 없음 |
| D3 | 선언 제약 | 6개 족보 중 **아무거나 선언 가능** (오픈 카드와 모순돼도 허용 — 거짓 선언의 일부) | UI에서 모순 여부는 표시하지 않음 |
| D4 | 오픈/선언 순서 | 선 플레이어 먼저 오픈+선언, 이어서 후 플레이어 | |
| D5 | 동순위 상성도 같을 때 | 나머지 카드 상성 → 그래도 같으면 **무승부** (트리플↔같은 트리플, 믹스↔믹스 포함) | 팟 이월 |
| D6 | 매치 종료 판정 | 라운드 시작 시 **앤티(1개)를 낼 수 없는 쪽이 패배** | 이월 팟이 남은 채 끝나는 엣지 케이스 방지 |
| D7 | 올인 이후 | 남은 베팅 단계는 생략하고 즉시 진행(오픈/쇼다운) | 통상 포커와 동일 |

---

## 2. 프로젝트 구조 (feast_of_memory 미러링)

```
LoveWinsAll.xcodeproj            Xcode 16+ / iOS 17+ / SwiftUI
LoveWinsAll/
├── App/LoveWinsAllApp.swift
├── Game/
│   ├── GameEngine.swift         규칙 상태기계 (순수 값 타입, Codable — 결정적 재현)
│   ├── HandEvaluator.swift      족보 판정·비교 (엔진에서 분리해 전수 테스트 용이하게)
│   ├── AIPlayer.swift           몬테카를로 승률 + 블러핑 모델 AI (난이도 3단계)
│   ├── GameViewModel.swift      대국 진행, 베팅 UI 상태, 연출 시퀀스, 타이머
│   ├── StatsStore.swift         전적 저장 (feast 재사용)
│   └── EngineSelfTest.swift     DEBUG 셀프테스트 (족보 전수 검증 + 시나리오 리플레이)
├── Online/
│   ├── RoomClient.swift         WebSocket 클라이언트 (feast 기반 + 딜링 메시지 추가)
│   └── OnlineConfig.swift
├── Monetization/                feast 그대로 재사용 (AdMob + 광고 제거 IAP)
└── Views/
    ├── HomeView.swift           1인용 / 2인용 / 연습 게임 / 규칙 / 전적 / 설정
    ├── GameView.swift           카드·칩·베팅 UI (아래 §5)
    ├── RulesView.swift
    ├── OnlineLobbyView.swift    방 생성 / 코드 입장 (feast 재사용)
    ├── StatsView.swift
    └── SettingsView.swift
server/
├── index.js                     온라인 서버 (릴레이 + 딜러 역할, 아래 §6)
└── test.js                      서버 통합 테스트
render.yaml / fastlane/ / .github/workflows/   feast 것을 이름만 바꿔 재사용
```

**feast_of_memory에서 거의 그대로 가져올 수 있는 것**: 서버 골격(방 코드/핑퐁/헬스체크),
RoomClient, OnlineLobbyView, StatsStore, Monetization 일체, CI·TestFlight 워크플로,
fastlane, render.yaml, App Store 문서. → 전체 작업량의 40% 가량 절약.

---

## 3. 게임 엔진 설계 (`GameEngine.swift`)

feast와 동일한 원칙: **순수 값 타입 상태기계**. `apply(move) throws -> MoveOutcome`
하나로 모든 상태 전이를 처리하고, 같은 무브 스트림이면 어느 기기에서든 동일 상태를 재현한다.

```swift
enum Card: Int, Codable { case scissors, rock, paper, love }

enum HandRank: Int, Comparable {          // 값이 작을수록 높은 족보
    case loveWinsAll = 1, triple, twoLove, mix, double, oneLove
}

enum Phase: Codable {
    case ante                  // 앤티 납부 + 카드 배분 대기
    case betting1              // 1차 베팅
    case declaration           // 카드 1장 오픈 + 족보 선언 (선 → 후)
    case betting2              // 2차 베팅
    case showdown              // 쇼다운 연출 후 정산
    case roundEnd              // 정산 완료 — 다음 라운드 or 매치 종료
    case finished
}

enum Move: Codable {
    case deal(hands: [[Card]])            // 라운드 시작: 양쪽 손패 (온라인은 서버가, 1인용은 로컬 RNG가 생성)
    case check
    case bet(Int)                          // 벳/레이즈 통합 (추가로 거는 칩 수)
    case call
    case fold
    case declare(open: Card, rank: HandRank)   // 손패 중 1장 오픈 + 족보 선언
    case forfeit
}
```

**엔진이 관리하는 상태**: 양쪽 손패 · 스택(칩) · 현재 팟 · 이월 팟 · 베팅 차례와 콜해야 할 금액 ·
선 플레이어 · 오픈된 카드와 선언 내용 · 라운드 번호 · 승자.

**베팅 상태기계** (1차/2차 공용): 선이 먼저 행동. `check-check` 또는 `bet…-call`이면 단계 종료,
`fold`면 즉시 라운드 종료(팟은 상대에게, 카드 비공개). 레이즈는 콜 금액 이상 추가. 칩이 모자라면
자동으로 올인 처리하고 초과 베팅분은 반환(D2). 양쪽 다 올인이면 이후 베팅 단계는 건너뛴다(D7).

**정산**: 쇼다운 승자가 팟+이월 팟 획득. 무승부면 전액 이월. 라운드 종료 시 앤티 불가능한 쪽 패배(D6).

### 족보 판정·비교 (`HandEvaluator.swift`)

엔진에서 분리한 순수 함수 두 개:

```swift
func evaluate(_ hand: [Card]) -> HandRank
func compare(_ a: [Card], _ b: [Card]) -> ComparisonResult   // 승/패/무
```

`compare` 로직: 족보 순위가 다르면 높은 쪽 승 → 같으면 족보 정의 카드끼리 상성 비교
(트리플/더블은 페어 타입, 믹스는 항상 무, 투 러브는 나머지 1장, 원 러브는 즉시 무) →
그래도 같으면 나머지 카드를 상성 순으로 비교 → 전부 같으면 무승부.

참고로 러브 윈즈 올(러브 3장)끼리의 대결은 카드가 4장뿐이라 불가능하고,
투 러브끼리는 4장 전부가 나뉘는 경우(2+2)만 가능 — 테스트 케이스에 포함한다.

---

## 4. AI 설계 (`AIPlayer.swift`)

feast의 AI가 "불완전한 기억"을 모델링했듯, 이 게임의 본질인 **승률 계산과 심리전**을 모델링한다.
AI는 엔진의 비공개 상태(상대 손패)를 절대 읽지 않고, 관찰 가능한 정보만 사용한다.

**핵심: 몬테카를로 승률 추정**
- 내 손패 3장을 제외한 27장에서 상대 손패를 수백 회 샘플링 → `compare`로 승/무/패 비율 계산
- 선언 단계 이후에는 상대의 오픈 카드를 조건으로 샘플링 (오픈 카드 1장 고정 + 나머지 26장에서 2장)
- 상대의 족보 선언은 난이도별 "신뢰도" 가중치로 반영 (선언과 부합하는 샘플에 가중)

**난이도 파라미터**

| | 쉬움 | 보통 | 어려움 |
|---|---|---|---|
| 샘플 수 / 판단 노이즈 | 적음 / 큼 | 중간 | 많음 / 작음 |
| 블러핑(약패로 벳/거짓 선언) 빈도 | 거의 없음 | 가끔 | 상황에 따라 |
| 상대 선언 신뢰도 | 곧이곧대로 믿음 | 중간 | 베팅 패턴과 교차 검증 |

**행동 결정**: 추정 승률을 팟 오즈와 비교해 폴드/콜/벳 결정 + 난이도별 랜덤성.
오픈할 카드 선택도 전략의 일부(강패는 숨기고 약패를 보여 블러핑, 혹은 그 반대).

---

## 5. UI / UX (`Views/`)

feast의 화면 구성(홈 → 대국 → 결과)을 유지하고, 대국 화면만 이 게임에 맞게 새로 만든다.

**GameView 레이아웃**
- 상단: 상대 영역 — 뒷면 카드 3장(오픈된 카드는 앞면), 선언 족보 말풍선, 칩 스택
- 중앙: 팟(+이월 팟 표시), 베팅 현황, 라운드/선 표시
- 하단: 내 카드 3장(항상 앞면), 내 칩, 행동 버튼

**주요 연출**
- 딜링(카드 날아오는 애니메이션), 카드 플립, 칩 이동(베팅/팟 획득)
- 선언 단계: 오픈할 카드 선택 → 족보 선택 시트 (거짓 선언 가능함을 안내)
- 베팅 단계: 체크/콜/폴드 버튼 + 레이즈 슬라이더(올인 포함)
- 쇼다운: 순차 카드 공개 → 족보 비교 하이라이트 → 승패 및 칩 이동
- 연습 게임 모드는 상단에 "연습" 배지 표시, 전적에 미집계

**턴 타이머**: 방송 규칙에는 없지만 온라인 대전의 잠수 방지를 위해 행동당 60초 제한(초과 시 체크/폴드 자동 처리). feast의 타이머 코드를 재사용.

---

## 6. 온라인 대전 (`server/` + `Online/`)

feast의 릴레이 서버 구조를 기반으로 하되, **한 가지 중요한 차이**가 있다:
feast는 모든 정보가 공개라 순수 릴레이로 충분했지만, 이 게임은 손패가 비공개다.
클라이언트가 카드를 생성해 공유하면 상대 앱 메모리에 내 패가 존재하게 되므로, **서버가 딜러 역할**을 맡는다.

**프로토콜 (feast 프로토콜 + 딜링 메시지)**
```
클라이언트 → 서버
  {"type":"create"} / {"type":"join","code":"ABC123"}   feast와 동일
  {"type":"move","move":{...}}                          베팅/선언 무브 릴레이 (서버는 해석 안 함)
  {"type":"ready"}                                      다음 라운드 준비 완료

서버 → 클라이언트
  {"type":"room"|"joined"|"start"|"left"|"error"}       feast와 동일
  {"type":"hand","cards":[...]}                         자기 손패만 비공개 전달
  {"type":"reveal","hands":[[...],[...]]}               쇼다운 시 양쪽 손패 공개
```

서버는 규칙을 해석하지 않고 **셔플·배분·공개만** 담당한다 (약 +60줄). 베팅 검증은
양쪽 클라이언트의 결정적 엔진이 수행 — feast와 같은 동기화 원리다.
선언 무브에 포함된 오픈 카드는 쇼다운 때 서버가 아는 실제 손패와 대조돼 변조 시 어긋나므로 자연 검증된다.

배포는 feast와 동일하게 Render 무료 티어 + `render.yaml`, 방 코드 6자리, 핑퐁 연결 관리 재사용.

---

## 7. 테스트 전략

feast의 "실제 방송 대국 리플레이 검증" 철학을 잇는다.

1. **족보 전수 테스트** — 30장 중 3장 조합은 C(30,3)=4,060가지뿐. 카드 타입 기준으로는 20가지 멀티셋.
   모든 손패 쌍(20×20)의 `evaluate`/`compare` 결과를 기대표와 대조하는 전수 테스트. 특히:
   - [가위,가위,러브] = 원 러브 (더블 아님)
   - 투 러브 ↔ 투 러브 (나머지 1장 비교), 원 러브 ↔ 원 러브 (즉시 무)
   - 같은 타입 트리플끼리 / 믹스끼리 = 무승부
2. **베팅 시나리오 테스트** — 체크-체크, 벳-레이즈-콜, 폴드, 올인(부족 콜 반환), 무승부 팟 이월,
   이월 팟이 쌓인 상태의 정산, 앤티 불능 패배까지 무브 스트림 리플레이로 검증
3. **방송 대국 리플레이** — 방송에서 확인 가능한 실제 라운드가 있으면 feast처럼 `EngineSelfTest`에 수록 (DEBUG 시작 시 자동 실행)
4. **AI 스모크 테스트** — AI vs AI 수천 판 자동 대국: 크래시 없음, 칩 총량 보존(50개), 평균 게임 길이 확인
5. **서버 통합 테스트** — feast의 `server/test.js` 확장: 방 생성/입장 + 딜링 비공개성(자기 패만 수신) + reveal 검증

---

## 8. 개발 마일스톤

| 단계 | 내용 | 산출물 |
|---|---|---|
| **M1. 엔진** | 카드/족보/비교 + 베팅 상태기계 + 라운드·매치 흐름 + 전수 테스트 | `GameEngine` `HandEvaluator` `EngineSelfTest` |
| **M2. 1인용** | AI 3단계 + ViewModel + 최소 UI로 로컬 대국 완주 (연습 게임 포함) | `AIPlayer` `GameViewModel` |
| **M3. UI 완성** | GameView 연출(딜링/플립/칩 이동/선언/쇼다운), 홈·규칙·전적·설정 | `Views/` 일체 |
| **M4. 온라인** | 서버(딜러 릴레이) + RoomClient 확장 + 로비 + 통합 테스트 + Render 배포 | `server/` `Online/` |
| **M5. 출시 준비** | 수익화 이식, CI/TestFlight 워크플로, 아이콘/스토어 문구, App Store 제출 | `fastlane` `.github/` `docs/` |

M1이 전체의 토대이므로 규칙 해석(§1의 D1~D7)을 M1 착수 전에 확정한다.
M2 완료 시점부터 실제 플레이가 가능하므로 규칙 체감 후 D1~D7을 재조정할 수 있다.

---

## 9. 리스크 및 참고

- **베팅 UX**: 포커식 베팅은 첫 화면 학습 비용이 있다 → 연습 게임(칩 20개)을 온보딩 튜토리얼로 활용하고, 행동 버튼에 콜 금액/팟 크기를 항상 병기
- **AI 밸런스**: 블러핑 빈도는 수치 조정이 어려운 영역 — M2에서 AI vs AI 대량 시뮬레이션으로 난이도별 승률 곡선을 측정해 튜닝
- **온라인 공정성**: 서버 딜링으로 손패 유출은 차단되지만, 클라이언트 변조까지 막는 서버 권위 검증은 범위 외 (친선전 수준으로 충분)
- **스크린샷 처리**: feast와 달리 이 게임은 기록이 반칙이 아니므로 스크린샷 감지 몰수패 로직은 **가져오지 않는다**
